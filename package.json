{
  "name": "find-property",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:db": "node scripts/setup.mjs",
    "scrape": "node --env-file .env.local scripts/scrape.mjs",
    "dev": "node --env-file .env.local --watch scripts/dev.mjs"
  },
  "devDependencies": {
    "vercel": "latest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
